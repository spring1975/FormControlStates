import { FormControlHintDirective } from './form-control-hint.directive';

describe('FormControlHintDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlHintDirective();
    expect(directive).toBeTruthy();
  });
});
